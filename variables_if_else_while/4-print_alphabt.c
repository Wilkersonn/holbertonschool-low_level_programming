#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
const char *message = "abcdefghijklmnopqrstuvwxyz\n";
const char *ptre = message;
while (*ptr != 'q' && *ptr != 'e')
{
ptr++;
}

while (*ptr)
{
putchar(*ptr);
ptr++;
}


return (0);
}

#include "main.h"
#include <stdio.h>


/**
 * mul - Verify if the char is digit or not
 *
 * @a: first int to be multiplicate
 * @b: second int to be multiplicate
 *
 * Return: 1 ou 0
 *
 */
int mul(int a, int b)
{
	int calcul;

	calcul = (a * b);
		return (calcul);
}
